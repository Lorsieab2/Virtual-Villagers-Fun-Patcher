outputs/
research/
.tools/
__pycache__/
*.pyc
patcher_local_settings.json
*.patch-log.json

# Local playtest stock-exe inputs (never commit game binaries)
inputs/

# build/staging artifacts (do not commit)
handoff-staging/
runtime-deps/
scratchpad_*.exe

# local build/test scratch outputs (never commit)
scratchpad/

# MSVC intermediates from the native companion builds (scripts/build_*_dll.ps1).
# The shipped product is the DLL under data/candidates/; these are byproducts.
*.obj
*.exp
*.lib
*.res
