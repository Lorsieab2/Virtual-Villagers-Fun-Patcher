# VV1/VV2 fullscreen owner-transition evidence gate

Status: **STOP**. This is a disabled evidence contract, not a patch, catalog entry, native-output path, or player certification. Its tracked evidence list is intentionally empty.

The gate covers Tech, Villager Detail, isolated Full Mastery, future Full Heal, and every result modal. It requires exact stock executable and complete game-folder/DLL fingerprints; a proved distinction between `SDL_Window*` and `HWND`; a valid same-process owner captured before transition and reused for every `DialogBoxParamA` and `MessageBoxA`; owner-monitor work-area centering and edge clamping; exact SDL flags and leave/enter ABIs; fresh singleton/outer/engine/window identities; preservation of the original target return; exactly one restore after any successful leave; TLS/POD destructor and uninstall cleanup; and exact destination-DLL/composition ownership.

The receipt matrix is 5 routes × 2 modes × 6 results: cancel, no-op, success, failure, foreground change, and restore failure. A future record needs all 60 receipts for each game, including owner/PID checks, identity snapshots, transition counts, result ownership, final flags, mutation/charge outcome, and truthful restore-failure disclosure.

`data/candidates/vv1_vv2_fullscreen_safe_candidate.json` is bound by exact SHA-256 only as a disabled static oracle. Its wrapper bytes are not runtime evidence and are explicitly rejected as enablement evidence. The gate has no operation that enables, publishes, emits, rebuilds, packages, or installs them.

Remaining gaps are the complete stock-folder inventories, exact native Win32/SDL transition and state-byte proof, identity and native-return proof, cleanup/destructor proof, exact hook/cave/append ownership and reverse uninstall receipts, and the complete windowed/fullscreen runtime matrix. Until repository-owned nonsynthetic evidence closes every gap, publication, player readiness, runtime certification, and native output remain false.
