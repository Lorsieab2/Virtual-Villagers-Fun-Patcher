# VV1-VV5 mask parity against the VV5 contract

Status date: 2026-08-29. This is an evidence ledger, not a completion claim.
VV5 is the sole behavioral reference. Each earlier game may use different
hooks, records, atlases, and draw ABIs, but the player-visible result must match
VV5 for positioning, render order, head tracking, screen-specific facing,
actions, selection, pickup/held/release, and persistence.

## Evidence labels

- **Confirmed static:** source/generated bytes bind the stated record, callsite,
  arguments, and ABI. This is not player-visible proof.
- **Player approved:** the player has explicitly reported the named live path
  working. Preserve it unless a new failure is reported.
- **Likely:** evidence suggests shared ownership, but the call graph or live path
  is not exhaustive.
- **Unknown:** no exact source/binary/runtime evidence currently binds the path.

## Surface matrix

| Game | Village | Map/overview | Details | Actions/poses | Selection | Pickup/held/release |
| --- | --- | --- | --- | --- | --- | --- |
| VV1 | Confirmed static: central loops and three head ABIs are gated | **Unknown:** UI/map clusters are intentionally outside the village gate | Confirmed static after the exact-seven-argument wrapper repair; runtime pending | Confirmed static for the audited head calls in both central loops; runtime pending | Likely ordinary village path; runtime pending | Static chain keeps the held record in the ordinary compositor; visibility/release pending player proof |
| VV2 | Confirmed static | **Unknown** | Confirmed static | Confirmed static across 68 audited head-atlas callsites | **Unknown** | **Unknown** |
| VV3 | Confirmed static for the normal world handler | **Unknown** | Hook confirmed static; portrait-facing result still needs player proof | Partial: action overlay is limited to selectors `0..50` and uses art-derived anchors; selectors beyond that boundary are not covered | **Unknown** | **Unknown:** no held hook; two previously suspected sites are timed effects without villager identity |
| VV4 | Confirmed static; player approved for the unchanged world hook | **Unknown** | Confirmed static at `0x45F702`; current Details repair still needs player acceptance | Player approved for observed walk/bend/sit/swim through the world hook; exhaustive call ownership is not independently enumerated | Included in prior player observation; exact state gate not separately mapped | Player approved for observed pickup through `0x468263`; no separate held hook is justified by current static evidence |
| VV5 | Confirmed static: Task9 brackets the native village render and restores transient faction fields | **Unknown** | Confirmed static: exact wrapper at `0x466E05`, portrait facing `record+0x2F3C mod 3`, ×1.5 scale, atlas `0x155` (3×5) | Likely when the action reaches the bracketed native renderer; no exhaustive action call graph currently proves every pose | **Unknown** | **Unknown:** active drag ownership and release path are not mapped in the current Task9 source |

## First divergence and the safe repair made

VV1 Details was the first confirmed behavioral divergence from the VV5 rule.
The old code captured only scale, then rebuilt portrait X/Y from fixed constants
and age buckets and obtained facing from a global. The current generator routes
all four native portrait head calls (`0x43741B`, `0x4374A4`, `0x437503`,
`0x437556`) through one ABI-compatible wrapper at `0x490720`. It duplicates and
replays the exact seven native arguments, preserves the exact renderer wrapper,
and passes the untouched tuple to `Vv1DrawPortraitMask`. The overlay changes only
the atlas/color row and applies a scale-aware art-registration lift. The village
renderer was not changed.

This repair is statically gated by `tests/test_vv1_mask_render_contract.py`.
It has not yet been accepted in a running game by the player.

## Review-gated hardening before the rc2 playtest

The GitHub review audit and its all-five follow-up found seven defects that were
still present after the earlier integration work. They are now repaired and
statically gated. A final adjacent-code audit then closed six additional
fail-closed and identity-boundary gaps:

1. VV1's Details wrapper records a permanent fail-open sentinel when the
   companion DLL or `Vv1DrawPortraitMask` export cannot be resolved, instead of
   retrying module/export resolution on every portrait head draw.
2. VV1's village-frame hook calls `Vv1MaskTick` after the one-shot restore. The
   DLL observes occupied slots, clears a non-zero mask only after a previously
   seen slot becomes free, and persists only an actual clear. This closes the
   load/picker-only record-reuse window in the generated path.
3. VV2 replaces an installed 320x440 mask atlas only when its dimensions, byte
   length, and SHA-256 match one of the four exact obsolete atlases previously
   bundled by this repository. Current art and arbitrary/custom 320x440 art are
   preserved; the replacement is published from a complete temporary file.
4. VV2 rejects sidecar save-slot values outside legacy slot 0 and numbered slots
   1..5 before formatting them, keeping the fixed `MAX_PATH` budget exact.
5. VV3 brackets every owned Grant Running preference mutation. It snapshots live
   fingerprints before the write and retags only matching masks afterward, so
   Grant Running no longer makes a mask disappear while slot-reuse protection is
   retained. The village-wide wrapper stays outside the crowded Origins payload,
   and a manifest-wide range test guards the certified native tails from overlap.
6. VV4 validates the complete sidecar and atlas paths, including the terminating
   NUL, before the first unbounded Win32 append. Overlong redirected Documents or
   executable-directory paths now fail open.
7. VV5 rejects truncated module paths and sidecar slots outside 0..5, and budgets
   the complete longest numbered sidecar path before any unbounded formatting.
8. VV1's shared mask getter rejects corrupt sidecar nibbles above the supported
   `0..5` range before a row can reach any renderer.
9. VV1 clears the patch-owned mask nibble at the exact stock newborn/allocation
   boundary (`0x43C393`) before record reuse. The clear is marked dirty and its
   sidecar write is retried until both persistence writes succeed, so a transient
   I/O failure cannot leave the in-memory slot silently divergent.
10. VV2 sanitizes every loaded sidecar byte to `0..5`; its adult/world and scaled
    child/Details consumers independently reject out-of-range values before atlas
    row subtraction.
11. VV3 sanitizes every loaded sidecar mask value to `0..5` before any table entry
    can reach a renderer.
12. VV4 validates the stored gender+name fingerprint before returning a mask. A
    prior completed present-path sweep must first make the slot identity-ready;
    this prevents the first load frame, before a name is initialized, from
    destructively invalidating a valid restored entry.
13. The VV4 identity-ready condition is reset on save-slot change, so the guarded
    invalidation rule is applied independently for each loaded village.

The VV1 whole-village command still uses the compositor's verified
`record+0x28 == 1` occupied predicate. Whether an occupied corpse must be
excluded for exact VV5 behavior is not proven by current VV5 evidence, so that
policy is deliberately unchanged. None of these static repairs is player-visible
runtime acceptance.

## Remaining blockers to a truthful all-five completion claim

1. Map/overview ownership is unmapped in all five games. VV1 explicitly excludes
   those callers; no other game has an exhaustive map route in current evidence.
2. VV5—the reference—does not yet have a mapped active pickup/held/release path in
   the current Task9 implementation. That path must be identified before it can be
   adapted safely to VV2, VV3, and any other game that does not inherit a proven
   central renderer.
3. VV3's action path has a hard `0..50` selector boundary. “Every action” cannot be
   claimed until the complete selector inventory is captured and classified.
4. VV2's exact same-frame free-to-newborn reuse path remains evidence-blocked. The
   known allocation routine (`sub_44C600`) has no reviewed proof that it is
   birth-only, and no stable VV2 identity field has been established, so no guessed
   birth detour is installed.
5. VV3's gender+Likes+Dislikes fingerprint can collide between villagers (including
   a replacement/newborn). No proven stable name, identity, or allocation-generation
   field exists in the current exact-build evidence. Removing the existing fallback
   would regress observed reload/slot-shift recovery, so this boundary requires a
   player trace or new native evidence rather than an invented offset.
6. VV2 and VV5 still need exhaustive action/pickup ownership audits; a central hook
   is not automatically a complete call graph.
7. Details positioning in VV1/VV3/VV4 and every unapproved surface still requires
   player-visible runtime acceptance. Hashes, disassembly, generated manifests, and
   passing tests are not substitutes.

## Required player trace/acceptance matrix

For each game, first confirm the patcher reports a successful apply and record the
exact patched executable/DLL/atlas hashes. Then test one masked child and adult (and
elder where available), at least three mask colors, and every visible facing through:

1. ordinary village walking and turning;
2. each available sit, bend, fish, swim, sleep/lie, work, celebration, nursing, and
   other game-specific action;
3. selection without pickup;
4. pickup, movement while held, and the first frame after release;
5. Villager Details open, idle turn, close, and reopen;
6. map/overview open and close—also report if that screen does not render villagers;
7. immediate first village frame after load, save/reload, and save-slot switching.

Report the first failing game/surface/action separately, with a screenshot or short
video and whether the head itself was visible. A held-only, map-only, Details-only,
or action-only failure points to a different owner and should not be “fixed” by
widening a village gate blindly.
